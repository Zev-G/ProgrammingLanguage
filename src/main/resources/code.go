x = new Object()[10];

/*
import java.nio.file.*;

scanner = new Scanner(System.in);

registerInstanceMethodsOf(scanner);

prompt();
input = nextLine();
while (!isExit(input)) {
    handle(input);
    prompt();
    input = nextLine();
}

isExit(text) {
    return text.equalsIgnoreCase("exit");
}

handle(text) {
    args = Arrays.asList(text.split(" "));
    if (args.get(0).equals("read")) {
        println(Files.readString(Path.of(text.substring(5))));
        return "";
    }
    println("Invalid command: \"" + text + "\"");
}

prompt() {
    print("Write a command ");
}
*/